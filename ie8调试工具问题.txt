今天用IE8的开发人员工具调试时发现按了N次F12，它就是不出来，任务栏上又是有它的标签的。
联想到IE有记忆关闭时窗口大小的特性，估计这个开发者工具也记住了它上一次关闭时的大小和位置。
可以这样解决：
1、先在任务栏上选中它，让它处理激活状态
2、按住Alt键，再按空格键。这时会出现一个菜单，不要动鼠标。
3、按一下M键，光标会变成四向箭头，这时再按方向键，把它移到屏幕中间。
4、用鼠标移到开发者工具的框边上，拉动调整大小，关掉后再按F12就不会隐藏了。
OK，搞定。